import { ArticleWagesModel } from './article-wages-model';

describe('ArticleWagesModel', () => {
  it('should create an instance', () => {
    expect(new ArticleWagesModel()).toBeTruthy();
  });
});
