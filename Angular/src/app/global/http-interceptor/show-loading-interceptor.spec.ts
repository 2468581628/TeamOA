import { ShowLoadingInterceptor } from './show-loading-interceptor';

describe('ShowLoadingInterceptor', () => {
  it('should create an instance', () => {
    expect(new ShowLoadingInterceptor()).toBeTruthy();
  });
});
